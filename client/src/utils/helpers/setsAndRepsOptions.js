export const repOptions = Array.from({length: 100}, (x, i) => {
    return {
        key: `${i}`,
        text: `${i}`,
        value: `${i}`
    }
})

export const weightOptions = Array.from({length: 1000}, (x, i) => {
    return {
        key: `${i}`,
        text: `${i}`,
        value: `${i}`
    }
})